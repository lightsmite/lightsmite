# Builder

Regenerate the profile SVGs (run from this directory):

    python build_svg_v2.py ..

Then run `today.py` from the repo root to fill in live stats.
`art_color.py` converts `avatar.png` into the color-quantized ASCII portrait.
